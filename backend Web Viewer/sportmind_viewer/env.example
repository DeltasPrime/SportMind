# Plantilla de variables de entorno
# Copia este archivo a .env y completa con tus credenciales reales

# Credenciales AWS IAM (crear en IAM → Users → Tu Usuario → Security credentials → Create access key)
AWS_ACCESS_KEY_ID=TU_ACCESS_KEY_AQUI
AWS_SECRET_ACCESS_KEY=TU_SECRET_KEY_AQUI

# Región de AWS
AWS_DEFAULT_REGION=sa-east-1

# Nombre del bucket S3
S3_BUCKET_NAME=userdata-sportmind

# Modo de operación (false = leer directo desde S3, true = usar API Gateway)
USE_API_GATEWAY=false

# Solo necesario si USE_API_GATEWAY=true
API_GATEWAY_BASE_URL=https://xc06hens94.execute-api.sa-east-1.amazonaws.com/prod
API_GATEWAY_API_KEY=TU_API_KEY_AQUI

